<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">
    <!-- 相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 存储卡读写 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".LaunchWelcomeActivity" android:theme="@style/FullScreen"
            android:launchMode="standard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".LoginForgetActivity" />
        <activity android:name=".LoginSQLiteActivity" />
        <activity android:name=".RegisterSQLiteActivity" />
        <activity android:name=".LaunchImproveActivity" android:theme="@style/FullScreen"/>
        <activity android:name=".DepartmentStoreActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".EvaluateGoodsActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".EvaluateDetailActivity" android:theme="@style/AppCompatTheme" />
        <activity android:name=".EvaluatePhotoActivity" android:theme="@style/AppCompatTheme" />
    </application>

</manifest>